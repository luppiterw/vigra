group 'vivi'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.5

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}

task hello << {
    println 'entry.hello task'
}

task initilizeDatabase
initilizeDatabase << {
    println 'connect to database'
}
initilizeDatabase << {
    println 'update database schema'
}
initilizeDatabase {
    println 'closure initilizeDatabase'
}